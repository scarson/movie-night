// ABOUTME: Next.js build configuration.
// ABOUTME: Empty — no custom config needed; Cloudflare adaptation happens via open-next.config.ts.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
